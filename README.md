# 2단계 - 칸반 보드 생성(보드)

## 기능 요구 사항
>칸반(Kanban)은 개발 단계를 거치면서 업무를 스케줄링하고 관리하는 시스템이다. 칸반은 이전 단계에서 일을 밀어 넣는 대신 다음 단계로 일을 끌어가는 것을 강조한다. 칸반은 업무를 시각화하고, 진행 중인 작업 수를 줄이고, 시스템을 통해 흐름을 극대화한다.
from 책 「애자일 조직은 이렇게 일합니다」

- 디자인 시안을 참고하여 칸반 보드를 구현한다.
- 새 태스크 생성 버튼에 모달을 연결한다.
- 각 상태별 태스크 개수를 트래킹한다.
- 상태(To-Do, In Progress, Done)별 태스크 개수가 노출된다.
- 전체 할 일 중 완료된 일의 비율을 계산한다.

## 프로그래밍 요구 사항
- Row와 Column, LazyRow와 LazyColumn 등 요구 사항에 적절한 컴포넌트를 선택한다.
- 재사용 가능한 컴포넌트에 대해 고민해본다.
- 적절한 테스트 방법을 활용하여 기능 요구 사항을 테스트한다.
- 모든 요구 사항이 테스트 가능하진 않다. 스스로 판단해서 구분한다.
- 특정 조건에 따라 Snackbar를 노출한다.

---

## 기능 구현 목록
- [x] 태스크 카드 객체 생성 로직
  - [x] TaskCardData 클래스 리펙터링
- [x] 전체 태스크 카드 저장 테이블
- [x] 태스크 상태 구분 로직
- [x] 태스크 완료율 계산 로직
- [x] 태스크 상태값 한정하기 (enum)
- [x] 태그 입력 사이 공백 처리 ("긴급,  , 버그")

## UI 구현 목록

### [x] Header 영역
- [x] 칸반 보드 Title
- [x] 태스크 완료율 Text
- [x] 완료율 Progress Bar
- [x] 새 태스크 생성 Button
  - [x] 클릭 시 TaskCardDataInput() 컴포저블 팝업
### [x] 태스크 카드 보드
- [x] State 라벨
- [x] 태스크 개수 Text
- [x] 태스크 카드 LazyColumn
### [x] 특정 이벤트에 반응하는 SnackBar
- 각 SnackBar는 일정 시간동안 유지된 후 제거 
- [x] 새 태스크 생성 완료
  - [x] 새로운 태스크가 추가되었습니다.
- [x] 태스크 생성 취소
  - [x] 태스크 추가가 취소되었습니다.

### [x] TaskCardDataInput 리펙터링
- [x] Stateless 적용
### [x] LabelAndContent 리펙터링
- [x] RowScope -> FlowRowScope

## 테스트 시나리오
### TaskCardTable Unit 테스트
1. [x] TODO 상태 태스크가 입력되면 todoTable에 저장된다.
2. [x] IN_PROGRESS 상태 태스크가 입력되면 inProgressTable에 저장된다
3. [x] DONE 상태 태스크가 입력되면 doneTable에 저장된다
4. [x] 입력된 태스크의 전채 개수를 알 수 있다
5. [x] 여러 태스크 중 DONE 상태인 task의 개수를 알 수 있다
6. [x] 입력 된 태스크의 완료율을 알 수 있다

### TaskCardDataInput UI 테스트
1. [x] TaskCardDataInput 실행 시 모든 컴포저블이 생성되어야 한다
2. [x] 제목이 입력되면 생성 버튼이 활성화 된다
3. [x] 제목이 입력되고 태그가 잘못 입력되면 생성 버튼이 활성화되지 않는다
4. [x] 각 상태 버튼을 누르면 해당하는 값이 저장된다
5. [x] 각 담당자 버튼을 누르면 해당하는 값이 

### TaskBoard UI 테스트
1. [x] 입력된 태스크의 카드가 보드에 표시된다
2. [x] 태스크의 개수가 보드 상단에 표시된다
3. [x] 입력된 모든 태스크는 스크롤을 통해 표시 되어야 한다

### KanbanBoard UI 테스트
1. [x] 함수 호출 시 모든 UI 요소가 생성된다
2. [x] 새 태스크 생성 버튼을 누르면 다이얼로그가 표시된다
3. [x] 취소 버튼을 누르면 다이얼로그가 닫히고 취소 스낵바가 표시된다
4. [] 새 태스크를 생성하면 생성 스낵바가 표시된다
5. [] 태스크 생성 후 완료율 텍스트가 변경된다

# 1차 피드백
## KanbanBoard.kt

- 헤더, 다이얼로그, 스낵바, 프로그래스바, 태스크 보드 3열을 모두 다루고 있음
- onDissmiss와 onCancel에서 동일한 스낵바 메시지를 보여주는 코드가 중복됨
- modifier 중복 사용 점검

## model, constant 패키지

- 이 프로젝트가 비대해지게 될 경우, 누군가 패키지 구조만 보고 모델을 찾을 때 찾을 수 있는가?

## State.kt

- enum 클래스에서 파라미터로 값을 가지게 된다면, 값이 고정되기 때문에 나중에 요구사항이 수정되면 코드 전반에 영향을 끼칠 수 있음
- 다국어 지원을 생각해보면 같은 state 객체에 대해 여러 결과를 받아야 하는데 파라미터로 고정해 놓으면 여러 상황에 대응하기 힘들어짐

## TaskCardDataInputState.kt

- 한 클래스가 너무 많은 일을 하고 있음 (UI 상태 보관, 변경 핸들러, 검증 위임)
- getCard 함수에서 데이터 반환과 상태 초기화 두 가지 일을 같이 하고 있음
- 역할을 분리했으면 좋겠음
- no new line at end of file도 해결하기 (파일 마지막 줄에 공백 추가)

## TaskCardTable.kt

- 각 테이블에 접근할 때마다 filter 함수가 호출되고 있음
- 각 필드에 접근할 때마다 filter 함수가 호출됨
- 파생 상태를 효율적으로 관리할 수 있는 방법이 무엇인가?
- retioOfDoneFloat의 함수에서 버그가 있음
    - toFloat가 마지막에 호출되고 있어 1 / 2의 경우 toFloat가 붙어있어도 0이 출력됨
    - 이 때문에 전체 task의 개수가 2개이고 donetask가 1개일 때 LinearProgressIndicator에 상태가 표시되지 않음

## KanbanBoardTest.kt

- Compose Dialog가 내부적으로 어떤 구조를 만드는지, 이것이 테스트 프레임워크 시멘틱 트리에 어떤 영향을 끼치는지 조사해보기
- Dialog가 어떤 형태이길래 show될 때 무조건 앞에 보이는지