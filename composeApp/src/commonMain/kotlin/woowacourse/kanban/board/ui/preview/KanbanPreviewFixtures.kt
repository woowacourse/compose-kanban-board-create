package woowacourse.kanban.board.ui.preview

import woowacourse.kanban.board.domain.model.Tag

/** preview title message **/
internal const val TITLE_EX = "LazyColumn 컴포넌트 구현"
internal const val TITLE_MAX_EX = "LazyColumn 컴포넌트 구현LazyColumn 컴포넌트 구현LazyColumn 컴포넌트 구현LazyColumn 컴포넌트 구현LazyColumn 컴포넌트 구현LazyColumn 컴포넌트 구현LazyColumn 컴포넌트 구현"

/** preview content message **/
internal const val CONTENT_EX = "세로 스크롤 가능한 리스트 컴포넌트를 만들고 성능 최적화를 적용합니다."
internal const val CONTENT_MAX_EX = "너무너무너무 긴 설명은 두 줄까지만 노출하고 말줄임표로 처리합니다 두 줄까지만 노너무너무너무 긴 설명은 두 줄까지만 노출하고 말줄임표로 처리합니다 두 줄까지만 노...너무너무너무 긴 설명은 두 줄까지만 노출하고 말줄임표로 처리합니다 두 줄까지만 노..."

/** preview name message **/
internal const val NAME_EX = "다이노"
internal const val NAME_MAX_EX = "다이노다이노다이노다이노다이노다이노다이노다이노다이노"

/** preview tag group message **/
val USER_GROUP_EX = listOf(Tag("컴포넌트"), Tag("성능"))
val USER_GROUP_MAX_EX = listOf(Tag("너무너무"), Tag("긴 태그"), Tag("최대로"), Tag("5자까지"), Tag("5개제한임"))
